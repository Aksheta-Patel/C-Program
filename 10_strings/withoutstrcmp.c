#include<stdio.h>
#include<string.h>
int main()
{ 
    int i;
    char a[30],b[30];
    printf(" enter two strings: ");
    scanf("%s%s",a,b);
    for(i=0;a[i]!=0||b[i]!=0;i++)
    {
        if(a[i]!=b[i])
        {
            printf(" strings are not equal");
        }

    }
    printf(" equal");
}


















































































































































































































































